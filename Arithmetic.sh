#!/bin/bash
#

read -p "Enter a value" a
read -p "Enter a value" b

echo "addition of 2 values are : $(( a + b ))"
echo "subtraction of 2 values are : $(( a - b ))"
echo "multiplication of 2 values are : $(( a * b ))"

